# Author: Paul Côté
# Last Change Author: Paul Côté
# Last Date Changed: 2022/06/10

type: google.api.Service
config_version: 3

http:
  rules:
    - selector: fmtrpc.Unlocker.Login
      post: "/v1/login"
      body: "*"
    - selector: fmtrpc.Unlocker.SetPassword
      post: "/v1/setpassword"
      body: "*"
    - selector: fmtrpc.Unlocker.ChangePassword
      post: "/v1/changepassword"
      body: "*"